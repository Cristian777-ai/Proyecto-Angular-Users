{
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd ui-app && npm install --include=dev && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
