{
  "name": "proyecto-angular-users",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd ui-app && npm install --include=dev && npm run build"
  },
  "engines": { "node": "18.x" },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  }
}
